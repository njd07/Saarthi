# ── Railway Postgres ──
DATABASE_URL=postgresql://postgres:password@localhost:5432/saarthi

# ── Auth ──
JWT_SECRET=change-me-to-a-256-bit-random-string

# ── AI: Google AI Studio (free tier, 15 RPM) ──
GEMINI_API_KEY=your-google-ai-studio-key

# ── AI: OpenRouter (fallback, free models available) ──
OPENROUTER_API_KEY=your-openrouter-key

# ── AI: Groq (tertiary fallback + Whisper STT) ──
GROQ_API_KEY=your-groq-api-key

# ── TTS: ElevenLabs ── (optional)
ELEVENLABS_API_KEY=your-elevenlabs-key

# ── Server ──
PORT=3001
FRONTEND_URL=http://localhost:5173
